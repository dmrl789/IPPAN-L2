# Example environment file (DO NOT put secrets in git)
#
# Copy to `.env` and fill as needed.

# Config selection
IPPAN_L2_CONFIG=./configs/local.toml

# L1 RPC (if your config uses env: references)
L1_BASE_URL=http://127.0.0.1:8080
# L1_API_KEY=REPLACE_ME

# Endpoint paths (paths only; do not include base URL)
# L1_ENDPOINT_CHAIN_STATUS=/REPLACE_ME
# L1_ENDPOINT_SUBMIT_BATCH=/REPLACE_ME
# L1_ENDPOINT_GET_INCLUSION=/REPLACE_ME/{id}
# L1_ENDPOINT_GET_FINALITY=/REPLACE_ME/{l1_tx_id}

# Security (required for staging/prod modes)
# Admin token for ops endpoints (minimum 32 characters)
# ADMIN_TOKEN=your-secure-admin-token-min-32-characters-long

# Logging
RUST_LOG=info

# ============================================================
# Security Mode Reference
# ============================================================
# NODE_SECURITY_MODE can be: devnet, staging, prod
#
# devnet (default):
#   - All endpoints enabled
#   - No authentication required
#   - Suitable for local development
#
# staging:
#   - Eth header submission requires auth
#   - Higher rate limits than prod
#
# prod:
#   - Devnet-only endpoints disabled (/m2m/topup, ledger ops)
#   - List proofs endpoints require auth
#   - Eth header submission requires auth
#   - Strict rate limits and payload caps

