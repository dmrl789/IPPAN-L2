# Example environment file (DO NOT put secrets in git)
#
# Copy to `.env` and fill as needed.

# Config selection
IPPAN_L2_CONFIG=./configs/local.toml

# L1 RPC (if your config uses env: references)
L1_BASE_URL=http://127.0.0.1:8080
# L1_API_KEY=REPLACE_ME

# Endpoint paths (paths only; do not include base URL)
# L1_ENDPOINT_CHAIN_STATUS=/REPLACE_ME
# L1_ENDPOINT_SUBMIT_BATCH=/REPLACE_ME
# L1_ENDPOINT_GET_INCLUSION=/REPLACE_ME/{id}
# L1_ENDPOINT_GET_FINALITY=/REPLACE_ME/{l1_tx_id}

# Logging
RUST_LOG=info

