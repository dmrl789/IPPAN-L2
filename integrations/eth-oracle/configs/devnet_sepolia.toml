[ippan]
rpc_url = "http://127.0.0.1:8080"   # placeholder DevNet RPC for IPPAN L1
poll_interval_ms = 10000           # 10 seconds
subject_type = "validator"         # or "handle"

[ethereum]
rpc_url = "env:ETH_RPC_URL"        # allow override via env
chain_id = 11155111                # Sepolia
oracle_contract_address = "0x0000000000000000000000000000000000000000" # to be updated after deploy

[security]
score_scale = 1000000              # fixed-point scale for scores if needed
max_updates_per_round = 100
